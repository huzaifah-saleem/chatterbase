name: chatterbase

services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./charts:/app/charts
      - ./conversations:/app/conversations
    restart: unless-stopped
